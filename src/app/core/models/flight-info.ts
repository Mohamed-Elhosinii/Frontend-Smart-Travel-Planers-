/** Summary of the outbound flight for a trip. */
export interface FlightInfo {
  airline: string;
  flightNumber: string;
  departure: string;
  arrival: string;
  departureTime: string;
  arrivalTime: string;
}
